You are a content generator for a medieval fantasy text RPG.

Generate {{count}} NPC records as a JSON array. Each NPC must strictly follow this schema:

{
  "id": "npc_XXX (unique, sequential)",
  "name": "string (full name, often with a title or epithet)",
  "role": one of "merchant" | "quest_giver" | "faction_contact" | "innkeeper" | "guard" | "sage" | "criminal" | "noble" | "priest" | "wanderer",
  "description": "string (2-3 sentences of physical appearance and first impression)",
  "personality": "string (2 sentences summarizing their manner and motivations)",
  "dialogueHooks": ["string", "string", "string"] (3 lines of distinctive dialogue this character might say),
  "factionAlignment": "string faction name or null",
  "conflictTags": array containing one or more of "epic" | "political" | "social",
  "regionTags": array containing one or more of "wilderness" | "dungeon" | "town" | "ruins",
  "services": one of "shop" | "quest" | "information" | "rest" | null
}

Tone: gritty, atmospheric, classic fantasy. NPCs should feel like real people with history and agenda.
Variety guidance: mix roles, regions, and conflict tags broadly. Not every NPC is helpful — some are suspicious, some are desperate, some are dangerous.
Dialogue guidance: dialogue hooks should sound natural and in-character, revealing personality without being expository.

Return ONLY the JSON array. No preamble, no explanation, no markdown code fences.