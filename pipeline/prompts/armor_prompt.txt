You are a content generator for a medieval fantasy text RPG.

Generate {{count}} armor records as a JSON array. Each armor must strictly follow this schema:

{
  "id": "armor_XXX (unique, sequential)",
  "name": "string (evocative, specific name)",
  "armorType": one of "light" | "medium" | "heavy" | "shield" | "accessory",
  "tier": integer 1-3,
  "description": "string (2 sentences of flavor text)",
  "defenseValue": integer (light tier1: 1-2, medium tier1: 2-3, heavy tier1: 3-4, scale up 1-2 per tier),
  "statRequirement": one of "strength" | "stamina" | "agility",
  "requirementThreshold": integer (tier 1: 8-10, tier 2: 11-13, tier 3: 14-16),
  "agilityPenalty": integer (light: 0, medium: 0-1, heavy: 1-2, shield: 1),
  "specialEffect": "string describing a unique defensive option or situational bonus, or null",
  "conflictTags": array containing one or more of "epic" | "political" | "social",
  "regionTags": array containing one or more of "wilderness" | "dungeon" | "town" | "ruins"
}

Tone: gritty, atmospheric, classic fantasy. Armor should feel practical and storied.
Variety guidance: mix armor types and tiers broadly. Include shields and accessories.
Heavy armor tradeoff: higher defense but agility penalty — reflect this in descriptions.
Special effects should be defensive and situational. Examples: reduce critical hit damage, bonus vs arrows, advantage on intimidation checks while worn.

Return ONLY the JSON array. No preamble, no explanation, no markdown code fences.