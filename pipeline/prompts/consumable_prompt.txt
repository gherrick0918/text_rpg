You are a content generator for a medieval fantasy text RPG.

Generate {{count}} consumable records as a JSON array. Each consumable must strictly follow this schema:

{
  "id": "consumable_XXX (unique, sequential)",
  "name": "string (evocative, specific name)",
  "tier": integer 1-3,
  "description": "string (2 sentences of flavor text)",
  "effectType": one of "heal" | "buff" | "debuff" | "utility",
  "effectValue": "string describing the mechanical effect (e.g. 'restore 1d8 Health', 'gain +2 to next attack roll', 'reveal hidden enemies for 3 rounds')",
  "duration": one of "instant" | "rounds" | "persistent",
  "conflictTags": array containing one or more of "epic" | "political" | "social",
  "regionTags": array containing one or more of "wilderness" | "dungeon" | "town" | "ruins"
}

Tone: gritty, atmospheric, classic fantasy. Consumables should feel alchemical, herbal, or arcane.
Variety guidance: mix effect types broadly. Not everything should be a healing potion. Include buffs, utilities, and unusual effects.
Tier guidance: tier 1 effects are modest, tier 2 are meaningful, tier 3 are powerful and rare.

Return ONLY the JSON array. No preamble, no explanation, no markdown code fences.